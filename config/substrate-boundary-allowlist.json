{
  "$comment": "TIN-2423 / ledger item 30: named-interface allowlist for the substrate-boundary validator. Every entry carries provenance (TIN-2398 guardrail #2). A blahaj-reach hit at an allowlisted exact path or beneath its directory boundary is a NAMED INTERFACE only for the listed reach classes, not a bleed. This overlay's real blahaj touchpoint (tenant MailDomain/MailAccount CRs, TIN-2379) is a k8s API reach via a namespace-scoped kubeconfig against mail.tinyland.dev/v1alpha1 CRDs, not a blahaj repo-tree reach, so it does not trip these patterns and needs no entry here. See docs/mail-cr-apply-runbook.md.",
  "schema_version": 1,
  "allowed": [
    {
      "path_prefix": "config/arc-storage-source-contract.json",
      "interface": "TIN-4072 source-only signed StorageClass provenance: tinyland-inc/blahaj@b56b1d69aaace4630bb400504bad50d2a033db09 with exact object IDs bound by the public-surface validator",
      "provenance": {
        "decision": "TIN-4072 signed ARC storage source contract; this repository reference is provenance only, never checkout, dispatch, filesystem, state, plan, apply, or runtime reach",
        "date": "2026-08-30"
      },
      "kinds": [
        "repo-ref"
      ]
    }
  ]
}
